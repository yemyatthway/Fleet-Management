# Fleet Management Roles API

This backend currently serves the Roles page only.

## Run the API

```bash
dotnet restore backend/FleetManagement.Api/FleetManagement.Api.csproj
dotnet run --project backend/FleetManagement.Api
```

The API listens on `http://localhost:5215`.
